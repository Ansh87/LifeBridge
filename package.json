{
  "name": "lifebridge",
  "version": "1.1.0",
  "private": true,
  "description": "LifeBridge, AI-powered crisis recovery. Static site on Netlify, Gemini via a serverless proxy, Firebase for auth and saved plans.",
  "license": "MIT",
  "scripts": {
    "dev": "npx --yes serve . -l 5173",
    "test:rules": "firebase emulators:exec --only firestore --project lifebridge-rules-test \"node tests/firestore.rules.test.mjs\"",
    "deploy:rules": "firebase deploy --only firestore:rules"
  },
  "dependencies": {},
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.0.0"
  }
}
